import React, { useEffect, useRef, useState } from 'react';
import axios from 'axios';

const SearchSpaceSearchInfo = () => {
  return (
    <div>
    </div>
  );
}

export default SearchSpaceSearchInfo;